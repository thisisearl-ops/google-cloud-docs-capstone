{
  "name": "google-cloud-docs-capstone",
  "private": true,
  "version": "0.1.0",
  "description": "Static HTML prototype for Google Cloud Docs Capstone (post-React migration).",
  "scripts": {
    "dev": "npx --yes serve@14 .",
    "start": "npx --yes serve@14 .",
    "build": "node ./scripts/build.mjs",
    "deploy": "npm run build && gh-pages -d dist -m \"chore: deploy site\"",
    "generate-skills": "node ./scripts/generate-skills-data.mjs"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
